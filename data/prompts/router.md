# Router Prompt Notes

Routing behavior in the next runtime is driven by router policies, patterns, and runtime code rather than a standalone markdown prompt template.

This file is documentation only.
