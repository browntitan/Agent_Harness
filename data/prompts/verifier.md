# Verifier Prompt Notes

Verifier behavior is defined by `verifier_agent.md` and the verifier execution contract in the runtime.

This file is documentation only and is not an active prompt template.
