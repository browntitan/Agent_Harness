# Grounded Answer Prompt Notes

The live grounded-answer template is `grounded_answer.txt`.

This markdown file is documentation only. The runtime uses the `.txt` template to render the evidence-backed JSON answer prompt consumed by synthesis code.
